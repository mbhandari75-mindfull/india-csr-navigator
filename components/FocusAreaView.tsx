'use client'
import { focusStyle, typeStyle } from '@/lib/colours'

interface Props {
  focusAreas: any[]
  orgs: any[]
  onSelect: (org: any) => void
}

export default function FocusAreaView({ focusAreas, orgs, onSelect }: Props) {
  return <div />
}
